openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Personal household budget API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
  - name: settings
  - name: incomes
  - name: categories
  - name: plan
  - name: transactions
  - name: rules
  - name: import
  - name: dashboard
  - name: reconciliation
  - name: export
  - name: investments
  - name: savings
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /settings:
    get:
      operationId: getSettings
      tags: [settings]
      summary: Get app settings
      responses:
        "200":
          description: Settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Settings"
    put:
      operationId: updateSettings
      tags: [settings]
      summary: Update app settings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SettingsUpdate"
      responses:
        "200":
          description: Updated settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Settings"
  /months:
    get:
      operationId: listMonths
      tags: [settings]
      summary: List budget months that have data, most recent first
      responses:
        "200":
          description: Months
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
  /incomes:
    get:
      operationId: listIncomes
      tags: [incomes]
      summary: List income sources
      responses:
        "200":
          description: Income sources
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/IncomeSource"
    post:
      operationId: createIncome
      tags: [incomes]
      summary: Create income source
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncomeSourceInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncomeSource"
  /incomes/{id}:
    patch:
      operationId: updateIncome
      tags: [incomes]
      summary: Update income source
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: number }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IncomeSourceUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IncomeSource"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
    delete:
      operationId: deleteIncome
      tags: [incomes]
      summary: Delete income source
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: number }
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /categories:
    get:
      operationId: listCategories
      tags: [categories]
      summary: List budget categories with their subcategories
      responses:
        "200":
          description: Categories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Category"
    post:
      operationId: createCategory
      tags: [categories]
      summary: Create category
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CategoryInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"
  /categories/{id}:
    patch:
      operationId: updateCategory
      tags: [categories]
      summary: Update category (rename or edit subcategory list)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: number }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CategoryUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Category"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
    delete:
      operationId: deleteCategory
      tags: [categories]
      summary: Delete category
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: number }
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /plan:
    get:
      operationId: listPlanLines
      tags: [plan]
      summary: List budget plan lines for a month
      parameters:
        - name: month
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Plan lines
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PlanLine"
    post:
      operationId: createPlanLine
      tags: [plan]
      summary: Create budget plan line
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlanLineInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlanLine"
  /plan/copy:
    post:
      operationId: copyPlan
      tags: [plan]
      summary: Copy all plan lines from one month to another
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CopyPlanInput"
      responses:
        "201":
          description: Copied plan lines
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PlanLine"
        "404":
          description: Source month has no plan lines
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
        "409":
          description: Target month already has plan lines
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /plan/{id}:
    patch:
      operationId: updatePlanLine
      tags: [plan]
      summary: Update budget plan line
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: number }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlanLineUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlanLine"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
    delete:
      operationId: deletePlanLine
      tags: [plan]
      summary: Delete budget plan line
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: number }
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /transactions:
    get:
      operationId: listTransactions
      tags: [transactions]
      summary: List transactions, optionally filtered
      parameters:
        - name: month
          in: query
          required: false
          schema: { type: string }
        - name: needsReview
          in: query
          required: false
          schema: { type: boolean }
        - name: source
          in: query
          required: false
          schema: { type: string, enum: [bank, manual] }
      responses:
        "200":
          description: Transactions
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Transaction"
    post:
      operationId: createTransaction
      tags: [transactions]
      summary: Add a manual transaction (cash spending etc.)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TransactionInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Transaction"
  /transactions/{id}:
    patch:
      operationId: updateTransaction
      tags: [transactions]
      summary: Categorize or edit a transaction; optionally save the choice as a rule
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: number }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TransactionUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Transaction"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
    delete:
      operationId: deleteTransaction
      tags: [transactions]
      summary: Delete a transaction
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: number }
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /import:
    post:
      operationId: importCsv
      tags: [import]
      summary: Import a bank CSV download (deduped, auto-categorized)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CsvImport"
      responses:
        "200":
          description: Import result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ImportResult"
        "400":
          description: Invalid CSV
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /imports:
    get:
      operationId: listImports
      tags: [import]
      summary: List past CSV imports (file, account, counts, months covered)
      responses:
        "200":
          description: Import history, newest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ImportBatch"
  /imports/{id}:
    delete:
      operationId: deleteImport
      tags: [import]
      summary: Delete an import record (only when none of its transactions remain)
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: number }
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
        "409":
          description: Import still has transactions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /rules:
    get:
      operationId: listRules
      tags: [rules]
      summary: List merchant categorization rules
      responses:
        "200":
          description: Rules
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Rule"
    post:
      operationId: createRule
      tags: [rules]
      summary: Create a merchant rule (retroactively applied to unreviewed transactions)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RuleInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Rule"
  /rules/{id}:
    delete:
      operationId: deleteRule
      tags: [rules]
      summary: Delete a merchant rule
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiMessage"
  /investments:
    get:
      operationId: getInvestmentOverview
      tags: [investments]
      summary: Get investment accounts, holdings, goals, allocations, and activity
      responses:
        "200":
          description: Investment workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentOverview"
  /investment/accounts:
    post:
      operationId: createInvestmentAccount
      tags: [investments]
      summary: Create an investment account
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvestmentAccountInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentAccount"
  /investment/accounts/{id}:
    patch:
      operationId: updateInvestmentAccount
      tags: [investments]
      summary: Update an investment account
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvestmentAccountUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentAccount"
    delete:
      operationId: deleteInvestmentAccount
      tags: [investments]
      summary: Delete an empty investment account
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204": { description: Deleted }
  /investment/holdings:
    post:
      operationId: createInvestmentHolding
      tags: [investments]
      summary: Create a manual holding
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvestmentHoldingInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentHolding"
  /investment/holdings/{id}:
    patch:
      operationId: updateInvestmentHolding
      tags: [investments]
      summary: Update a manual holding
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvestmentHoldingUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentHolding"
    delete:
      operationId: deleteInvestmentHolding
      tags: [investments]
      summary: Delete a holding
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204": { description: Deleted }
  /investment/transactions:
    post:
      operationId: createInvestmentTransaction
      tags: [investments]
      summary: Add a manual investment transaction
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvestmentTransactionInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentTransaction"
  /investment/transactions/{id}:
    patch:
      operationId: updateInvestmentTransaction
      tags: [investments]
      summary: Update a manual investment transaction
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvestmentTransactionUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentTransaction"
    delete:
      operationId: deleteInvestmentTransaction
      tags: [investments]
      summary: Delete a manual investment transaction
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204": { description: Deleted }
  /investment/goals:
    post:
      operationId: createInvestmentGoal
      tags: [investments]
      summary: Create an investment goal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvestmentGoalInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentGoal"
  /investment/goals/{id}:
    patch:
      operationId: updateInvestmentGoal
      tags: [investments]
      summary: Update an investment goal
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvestmentGoalUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentGoal"
    delete:
      operationId: deleteInvestmentGoal
      tags: [investments]
      summary: Delete an investment goal
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204": { description: Deleted }
  /investment/allocations:
    post:
      operationId: createInvestmentTargetAllocation
      tags: [investments]
      summary: Create an investment target allocation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvestmentTargetAllocationInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentTargetAllocation"
  /investment/allocations/{id}:
    patch:
      operationId: updateInvestmentTargetAllocation
      tags: [investments]
      summary: Update an investment target allocation
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InvestmentTargetAllocationUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentTargetAllocation"
    delete:
      operationId: deleteInvestmentTargetAllocation
      tags: [investments]
      summary: Delete an investment target allocation
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204": { description: Deleted }
  /investment/plan:
    post:
      operationId: syncInvestmentContributionPlan
      tags: [investments]
      summary: Sync the current and next budget cycles to the investment allocation plan
      responses:
        "200":
          description: Budget plan synchronized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InvestmentPlanSyncResult"
  /savings:
    get:
      operationId: getSavingsOverview
      tags: [savings]
      summary: Get household savings goals, contribution history, projections, and affordability
      responses:
        "200":
          description: Savings workspace
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavingsOverview"
  /savings/goals:
    post:
      operationId: createSavingsGoal
      tags: [savings]
      summary: Create a household savings goal
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SavingsGoalInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavingsGoal"
  /savings/goals/{id}:
    get:
      operationId: getSavingsGoal
      tags: [savings]
      summary: Get one household savings goal with its contribution history
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Savings goal
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavingsGoal"
        "404": { description: Not found }
    patch:
      operationId: updateSavingsGoal
      tags: [savings]
      summary: Update a household savings goal or lifecycle status
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SavingsGoalUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavingsGoal"
        "404": { description: Not found }
    delete:
      operationId: deleteSavingsGoal
      tags: [savings]
      summary: Delete a household savings goal and its contribution history
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204": { description: Deleted }
        "404": { description: Not found }
  /savings/goals/{id}/contributions:
    get:
      operationId: listSavingsContributions
      tags: [savings]
      summary: List auditable contribution history for a savings goal
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      responses:
        "200":
          description: Contribution history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/SavingsContribution"
        "404": { description: Not found }
    post:
      operationId: createSavingsContribution
      tags: [savings]
      summary: Add a contribution or balance adjustment to a savings goal
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SavingsContributionInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavingsContribution"
        "404": { description: Not found }
  /savings/goals/{id}/contributions/{contributionId}:
    patch:
      operationId: updateSavingsContribution
      tags: [savings]
      summary: Update a savings contribution or adjustment
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
        - name: contributionId
          in: path
          required: true
          schema: { type: integer }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SavingsContributionUpdate"
      responses:
        "200":
          description: Updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SavingsContribution"
        "404": { description: Not found }
    delete:
      operationId: deleteSavingsContribution
      tags: [savings]
      summary: Delete a savings contribution or adjustment
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: integer }
        - name: contributionId
          in: path
          required: true
          schema: { type: integer }
      responses:
        "204": { description: Deleted }
        "404": { description: Not found }
  /dashboard:
    get:
      operationId: getDashboard
      tags: [dashboard]
      summary: Dashboard summary for a budget month
      parameters:
        - name: month
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Dashboard summary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DashboardSummary"
  /reconciliation:
    get:
      operationId: getReconciliation
      tags: [reconciliation]
      summary: Manual vs bank actuals comparison per category for a budget month
      parameters:
        - name: month
          in: query
          required: false
          schema: { type: string }
      responses:
        "200":
          description: Reconciliation rows
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReconciliationRow"
  /export:
    get:
      operationId: exportWorkbook
      tags: [export]
      summary: Download the budget as an .xlsx workbook in the original layout
      responses:
        "200":
          description: Excel workbook
          content:
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status
    ApiMessage:
      type: object
      required: [error]
      properties:
        error: { type: string }
    Settings:
      type: object
      required: [selectedMonth, monthStartDay, checkingBuffer, debtStrategy]
      properties:
        selectedMonth: { type: string }
        monthStartDay: { type: number }
        checkingBuffer: { type: number }
        debtStrategy: { type: string }
    SettingsUpdate:
      type: object
      properties:
        selectedMonth: { type: string }
        monthStartDay: { type: number, minimum: 1, maximum: 28 }
        checkingBuffer: { type: number }
        debtStrategy: { type: string }
    IncomeSource:
      type: object
      required: [id, name, frequency, netAmount, monthlyEquivalent]
      properties:
        id: { type: number }
        name: { type: string }
        owner: { type: ["string", "null"] }
        frequency: { type: string }
        netAmount: { type: number }
        monthlyEquivalent: { type: number }
        notes: { type: ["string", "null"] }
    IncomeSourceInput:
      type: object
      required: [name, frequency, netAmount, monthlyEquivalent]
      properties:
        name: { type: string, minLength: 1 }
        owner: { type: ["string", "null"] }
        frequency: { type: string }
        netAmount: { type: number }
        monthlyEquivalent: { type: number }
        notes: { type: ["string", "null"] }
    IncomeSourceUpdate:
      type: object
      properties:
        name: { type: string, minLength: 1 }
        owner: { type: ["string", "null"] }
        frequency: { type: string }
        netAmount: { type: number }
        monthlyEquivalent: { type: number }
        notes: { type: ["string", "null"] }
    Category:
      type: object
      required: [id, name, subcategories, sortOrder]
      properties:
        id: { type: number }
        name: { type: string }
        subcategories:
          type: array
          items: { type: string }
        sortOrder: { type: number }
    CategoryInput:
      type: object
      required: [name]
      properties:
        name: { type: string, minLength: 1 }
        subcategories:
          type: array
          items: { type: string }
        sortOrder: { type: number }
    CategoryUpdate:
      type: object
      properties:
        name: { type: string, minLength: 1 }
        subcategories:
          type: array
          items: { type: string }
        sortOrder: { type: number }
    PlanLine:
      type: object
      required: [id, month, category, subcategory, planned]
      properties:
        id: { type: number }
        month: { type: string }
        category: { type: string }
        subcategory: { type: string }
        planned: { type: number }
        priority: { type: ["string", "null"] }
        fixedVariable: { type: ["string", "null"] }
        dueDay: { type: ["number", "null"] }
        notes: { type: ["string", "null"] }
    PlanLineInput:
      type: object
      required: [category, subcategory, planned]
      properties:
        month: { type: string, minLength: 1 }
        category: { type: string, minLength: 1 }
        subcategory: { type: string, minLength: 1 }
        planned: { type: number }
        priority: { type: ["string", "null"] }
        fixedVariable: { type: ["string", "null"] }
        dueDay: { type: ["number", "null"] }
        notes: { type: ["string", "null"] }
    CopyPlanInput:
      type: object
      required: [from, to]
      properties:
        from: { type: string, minLength: 1 }
        to: { type: string, minLength: 1 }
    PlanLineUpdate:
      type: object
      properties:
        category: { type: string, minLength: 1 }
        subcategory: { type: string, minLength: 1 }
        planned: { type: number }
        priority: { type: ["string", "null"] }
        fixedVariable: { type: ["string", "null"] }
        dueDay: { type: ["number", "null"] }
        notes: { type: ["string", "null"] }
    Transaction:
      type: object
      required: [id, date, description, amount, status, source, include, month, needsReview]
      properties:
        id: { type: number }
        date: { type: string }
        description: { type: string }
        originalDescription: { type: ["string", "null"] }
        bankCategory: { type: ["string", "null"] }
        amount: { type: number }
        status: { type: string }
        account: { type: ["string", "null"] }
        source: { type: string }
        category: { type: ["string", "null"] }
        subcategory: { type: ["string", "null"] }
        include: { type: boolean }
        month: { type: string }
        note: { type: ["string", "null"] }
        needsReview: { type: boolean }
        linkedBankId: { type: ["number", "null"] }
    TransactionInput:
      type: object
      required: [date, description, amount]
      properties:
        date: { type: string }
        description: { type: string, minLength: 1 }
        amount: { type: number }
        category: { type: ["string", "null"] }
        subcategory: { type: ["string", "null"] }
        account: { type: ["string", "null"] }
        note: { type: ["string", "null"] }
    TransactionUpdate:
      type: object
      properties:
        category: { type: ["string", "null"] }
        subcategory: { type: ["string", "null"] }
        include: { type: boolean }
        note: { type: ["string", "null"] }
        saveRule: { type: boolean }
        rulePattern: { type: string }
    CsvImport:
      type: object
      required: [csvContent]
      properties:
        csvContent: { type: string, minLength: 1 }
        fileName: { type: ["string", "null"] }
        account: { type: ["string", "null"] }
    ImportResult:
      type: object
      required: [totalRows, added, duplicates, pendingReplaced, autoCategorized, needsReview]
      properties:
        account: { type: string }
        totalRows: { type: number }
        added: { type: number }
        duplicates: { type: number }
        pendingReplaced: { type: number }
        autoCategorized: { type: number }
        needsReview: { type: number }
        importId: { type: number }
    ImportBatch:
      type: object
      required: [id, fileName, account, totalRows, added, duplicates, importedAt, months]
      properties:
        id: { type: number }
        fileName: { type: ["string", "null"] }
        account: { type: string }
        totalRows: { type: number }
        added: { type: number }
        duplicates: { type: number }
        importedAt: { type: string }
        months:
          type: array
          items: { type: string }
    Rule:
      type: object
      required: [id, pattern, category, subcategory]
      properties:
        id: { type: number }
        pattern: { type: string }
        category: { type: string }
        subcategory: { type: string }
    RuleInput:
      type: object
      required: [pattern, category, subcategory]
      properties:
        pattern: { type: string, minLength: 2 }
        category: { type: string, minLength: 1 }
        subcategory: { type: string, minLength: 1 }
    InvestmentAccount:
      type: object
      required: [id, name, institution, accountType, cashBalance, source]
      properties:
        id: { type: number }
        name: { type: string }
        institution: { type: string }
        accountType: { type: string }
        cashBalance: { type: number }
        source: { type: string }
    InvestmentAccountInput:
      type: object
      required: [name, institution, accountType, cashBalance]
      properties:
        name: { type: string, minLength: 1 }
        institution: { type: string, minLength: 1 }
        accountType: { type: string, minLength: 1 }
        cashBalance: { type: number }
    InvestmentAccountUpdate:
      type: object
      properties:
        name: { type: string, minLength: 1 }
        institution: { type: string, minLength: 1 }
        accountType: { type: string, minLength: 1 }
        cashBalance: { type: number }
    InvestmentHolding:
      type: object
      required: [id, accountId, securityId, ticker, securityName, shares, averageCost, costBasis, currentPrice, currentMarketValue, unrealizedGainLoss, unrealizedGainLossPercent, portfolioAllocationPercent]
      properties:
        id: { type: number }
        accountId: { type: number }
        securityId: { type: number }
        ticker: { type: string }
        securityName: { type: string }
        shares: { type: number }
        averageCost: { type: number }
        costBasis: { type: number }
        currentPrice: { type: number }
        currentMarketValue: { type: number }
        unrealizedGainLoss: { type: number }
        unrealizedGainLossPercent: { type: number }
        portfolioAllocationPercent: { type: number }
    InvestmentHoldingInput:
      type: object
      required: [accountId, ticker, securityName, shares, averageCost, currentPrice]
      properties:
        accountId: { type: number }
        ticker: { type: string, minLength: 1 }
        securityName: { type: string, minLength: 1 }
        shares: { type: number, minimum: 0 }
        averageCost: { type: number, minimum: 0 }
        currentPrice: { type: number, minimum: 0 }
    InvestmentHoldingUpdate:
      type: object
      properties:
        ticker: { type: string, minLength: 1 }
        securityName: { type: string, minLength: 1 }
        shares: { type: number, minimum: 0 }
        averageCost: { type: number, minimum: 0 }
        currentPrice: { type: number, minimum: 0 }
    InvestmentTransaction:
      type: object
      required: [id, accountId, date, month, transactionType, amount]
      properties:
        id: { type: number }
        accountId: { type: number }
        securityId: { type: ["number", "null"] }
        ticker: { type: ["string", "null"] }
        date: { type: string }
        month: { type: string }
        transactionType: { type: string, enum: [deposit, withdrawal, buy, sell, dividend, dividend_reinvestment, fee] }
        amount: { type: number }
        shares: { type: ["number", "null"] }
        price: { type: ["number", "null"] }
        notes: { type: ["string", "null"] }
    InvestmentTransactionInput:
      type: object
      required: [accountId, date, transactionType, amount]
      properties:
        accountId: { type: number }
        securityId: { type: ["number", "null"] }
        date: { type: string }
        transactionType: { type: string, enum: [deposit, withdrawal, buy, sell, dividend, dividend_reinvestment, fee] }
        amount: { type: number, minimum: 0 }
        shares: { type: ["number", "null"], minimum: 0 }
        price: { type: ["number", "null"], minimum: 0 }
        notes: { type: ["string", "null"] }
    InvestmentTransactionUpdate:
      type: object
      properties:
        accountId: { type: number }
        securityId: { type: ["number", "null"] }
        date: { type: string }
        transactionType: { type: string, enum: [deposit, withdrawal, buy, sell, dividend, dividend_reinvestment, fee] }
        amount: { type: number, minimum: 0 }
        shares: { type: ["number", "null"], minimum: 0 }
        price: { type: ["number", "null"], minimum: 0 }
        notes: { type: ["string", "null"] }
    InvestmentGoal:
      type: object
      required: [id, name, targetAmount, monthlyPlannedContribution, targetDate, currentPortfolioValue, contributionsToDate, investmentGrowth, remainingAmount, percentComplete, monthlyContributionProgress]
      properties:
        id: { type: number }
        accountId: { type: ["number", "null"] }
        name: { type: string }
        targetAmount: { type: number }
        monthlyPlannedContribution: { type: number }
        targetDate: { type: string }
        currentPortfolioValue: { type: number }
        contributionsToDate: { type: number }
        investmentGrowth: { type: number }
        remainingAmount: { type: number }
        percentComplete: { type: number }
        monthlyContributionProgress: { type: number }
    InvestmentGoalInput:
      type: object
      required: [name, targetAmount, monthlyPlannedContribution, targetDate]
      properties:
        accountId: { type: ["number", "null"] }
        name: { type: string, minLength: 1 }
        targetAmount: { type: number, minimum: 0 }
        monthlyPlannedContribution: { type: number, minimum: 0 }
        targetDate: { type: string }
    InvestmentGoalUpdate:
      type: object
      properties:
        accountId: { type: ["number", "null"] }
        name: { type: string, minLength: 1 }
        targetAmount: { type: number, minimum: 0 }
        monthlyPlannedContribution: { type: number, minimum: 0 }
        targetDate: { type: string }
    InvestmentTargetAllocation:
      type: object
      required: [id, accountId, securityId, ticker, securityName, monthlyAmount]
      properties:
        id: { type: number }
        accountId: { type: number }
        securityId: { type: number }
        ticker: { type: string }
        securityName: { type: string }
        monthlyAmount: { type: number }
    InvestmentTargetAllocationInput:
      type: object
      required: [accountId, ticker, securityName, monthlyAmount]
      properties:
        accountId: { type: number }
        ticker: { type: string, minLength: 1 }
        securityName: { type: string, minLength: 1 }
        monthlyAmount: { type: number, minimum: 0 }
    InvestmentTargetAllocationUpdate:
      type: object
      properties:
        monthlyAmount: { type: number, minimum: 0 }
    InvestmentSummary:
      type: object
      required: [portfolioValue, monthlyContribution, netContributions, investmentGrowth, goalProgress]
      properties:
        portfolioValue: { type: number }
        monthlyContribution: { type: number }
        netContributions: { type: number }
        investmentGrowth: { type: number }
        goalProgress: { type: number }
    InvestmentOverview:
      type: object
      required: [summary, accounts, holdings, transactions, goals, allocations]
      properties:
        summary:
          $ref: "#/components/schemas/InvestmentSummary"
        accounts:
          type: array
          items:
            $ref: "#/components/schemas/InvestmentAccount"
        holdings:
          type: array
          items:
            $ref: "#/components/schemas/InvestmentHolding"
        transactions:
          type: array
          items:
            $ref: "#/components/schemas/InvestmentTransaction"
        goals:
          type: array
          items:
            $ref: "#/components/schemas/InvestmentGoal"
        allocations:
          type: array
          items:
            $ref: "#/components/schemas/InvestmentTargetAllocation"
    InvestmentPlanSyncResult:
      type: object
      required: [message]
      properties:
        message: { type: string }
    SavingsContribution:
      type: object
      required: [id, goalId, amount, contributionDate, entryType, createdAt]
      properties:
        id: { type: number }
        goalId: { type: number }
        amount: { type: number }
        contributionDate: { type: string, format: date }
        entryType: { type: string, enum: [contribution, adjustment] }
        note: { type: ["string", "null"] }
        createdAt: { type: string }
    SavingsContributionInput:
      type: object
      required: [amount, contributionDate]
      properties:
        amount: { type: number, minimum: -10000000, maximum: 10000000 }
        contributionDate: { type: string, format: date }
        entryType: { type: string, enum: [contribution, adjustment] }
        note: { type: ["string", "null"], maxLength: 500 }
    SavingsContributionUpdate:
      type: object
      properties:
        amount: { type: number, minimum: -10000000, maximum: 10000000 }
        contributionDate: { type: string, format: date }
        entryType: { type: string, enum: [contribution, adjustment] }
        note: { type: ["string", "null"], maxLength: 500 }
    SavingsTrajectoryPoint:
      type: object
      required: [date, expected, actual]
      properties:
        date: { type: string, format: date }
        expected: { type: number }
        actual: { type: number }
    SavingsGoal:
      type: object
      required: [id, name, targetAmount, startingBalance, startDate, targetDate, priority, status, currentBalance, contributionsToDate, remainingAmount, percentComplete, timeProgressPercent, expectedBalance, variance, monthsRemaining, requiredMonthlyContribution, averageMonthlyContribution, projectedBalance, trajectoryStatus, recommendation, trajectory, contributions]
      properties:
        id: { type: number }
        name: { type: string }
        targetAmount: { type: number }
        startingBalance: { type: number }
        monthlyPlannedContribution: { type: ["number", "null"] }
        startDate: { type: string, format: date }
        targetDate: { type: string, format: date }
        priority: { type: string, enum: [High, Medium, Low] }
        status: { type: string, enum: [active, paused, completed, archived] }
        notes: { type: ["string", "null"] }
        currentBalance: { type: number }
        contributionsToDate: { type: number }
        remainingAmount: { type: number }
        percentComplete: { type: number }
        timeProgressPercent: { type: number }
        expectedBalance: { type: number }
        variance: { type: number }
        monthsRemaining: { type: number }
        requiredMonthlyContribution: { type: number }
        averageMonthlyContribution: { type: number }
        projectedBalance: { type: number }
        projectedCompletionDate: { type: ["string", "null"], format: date }
        trajectoryStatus: { type: string, enum: [Ahead, On track, Slightly behind, Behind, Overdue, Completed, Insufficient history] }
        recommendation: { type: string }
        trajectory:
          type: array
          items:
            $ref: "#/components/schemas/SavingsTrajectoryPoint"
        contributions:
          type: array
          items:
            $ref: "#/components/schemas/SavingsContribution"
    SavingsGoalInput:
      type: object
      required: [name, targetAmount, startingBalance, startDate, targetDate, priority]
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        targetAmount: { type: number, minimum: 0.01, maximum: 21474836.47 }
        startingBalance: { type: number, minimum: 0, maximum: 21474836.47 }
        monthlyPlannedContribution: { type: ["number", "null"], minimum: 0, maximum: 21474836.47 }
        startDate: { type: string, format: date }
        targetDate: { type: string, format: date }
        priority: { type: string, enum: [High, Medium, Low] }
        notes: { type: ["string", "null"], maxLength: 1000 }
    SavingsGoalUpdate:
      type: object
      properties:
        name: { type: string, minLength: 1, maxLength: 120 }
        targetAmount: { type: number, minimum: 0.01, maximum: 21474836.47 }
        startingBalance: { type: number, minimum: 0, maximum: 21474836.47 }
        monthlyPlannedContribution: { type: ["number", "null"], minimum: 0, maximum: 21474836.47 }
        startDate: { type: string, format: date }
        targetDate: { type: string, format: date }
        priority: { type: string, enum: [High, Medium, Low] }
        status: { type: string, enum: [active, paused, completed, archived] }
        notes: { type: ["string", "null"], maxLength: 1000 }
    SavingsSummary:
      type: object
      required: [activeGoalCount, totalCurrentBalance, totalTargetAmount, combinedMonthlyNeed, affordabilityStatus, projectedMonthlySurplus, primaryGoal]
      properties:
        activeGoalCount: { type: number }
        totalCurrentBalance: { type: number }
        totalTargetAmount: { type: number }
        combinedMonthlyNeed: { type: number }
        affordabilityStatus: { type: string, enum: [Within budget, Over budget, No budget data] }
        projectedMonthlySurplus: { type: ["number", "null"] }
        primaryGoal:
          oneOf:
            - $ref: "#/components/schemas/SavingsGoal"
            - type: "null"
    SavingsOverview:
      type: object
      required: [goals, summary]
      properties:
        goals:
          type: array
          items:
            $ref: "#/components/schemas/SavingsGoal"
        summary:
          $ref: "#/components/schemas/SavingsSummary"
    CategoryActual:
      type: object
      required: [category, planned, actual, remaining]
      properties:
        category: { type: string }
        planned: { type: number }
        actual: { type: number }
        remaining: { type: number }
    DashboardSummary:
      type: object
      required: [month, incomePlanned, incomeActual, plannedExpenses, actualExpenses, remaining, cashFlow, reviewCount, pendingCount, byCategory, investment, savings]
      properties:
        month: { type: string }
        incomePlanned: { type: number }
        incomeActual: { type: number }
        plannedExpenses: { type: number }
        actualExpenses: { type: number }
        remaining: { type: number }
        cashFlow: { type: number }
        reviewCount: { type: number }
        pendingCount: { type: number }
        byCategory:
          type: array
          items:
            $ref: "#/components/schemas/CategoryActual"
        investment:
          $ref: "#/components/schemas/InvestmentSummary"
        savings:
          $ref: "#/components/schemas/SavingsSummary"
    ReconciliationRow:
      type: object
      required: [category, planned, manualTotal, bankTotal, difference, budgetVariance, budgetStatus, status]
      properties:
        category: { type: string }
        planned: { type: number }
        manualTotal: { type: number }
        bankTotal: { type: number }
        difference: { type: number }
        budgetVariance: { type: number }
        budgetStatus: { type: string }
        status: { type: string }
